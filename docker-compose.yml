services:
  v2v_web_app:
    build: 
      context: .
      dockerfile: Dockerfile
    environment:
      - OLLAMA_HOST=http://ollama-api:11434
    networks:
      - internal-shared
    deploy:
      resources:
        limits:
          memory: 2G

networks:
  internal-shared: